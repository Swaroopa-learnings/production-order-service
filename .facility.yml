version: 1
repositories:
  primary: "github.com/Swaroopa-learnings/production-order-service"
  related: []
environment:
  start: docker compose up -d --build
  ready: curl --fail --silent --show-error --max-time 5 http://127.0.0.1:8080/actuator/health
  secrets: - OPENAI_API_KEY
  services:
    app:
      port: 8080
      protocol: http
      websocket: false
