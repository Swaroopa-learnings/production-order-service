version: 1
repositories:
  primary: "github.com/Swaroopa-learnings/production-order-service"
  related: []
environment:
  start: docker compose up -d --build
  services:
    app:
      port: 8080
      protocol: http
      websocket: false
